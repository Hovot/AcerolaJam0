/// @description 

var leftX = 0
var rightX = 100
var upY = 250
var lowerY = 300
var spacing = 65
var mainColor = c_purple
var edgeColor = c_fuchsia

if(burgerPower){
	draw_rectangle_color(leftX, upY + spacing*0, rightX, lowerY + spacing*0, mainColor, edgeColor, mainColor, mainColor, false)
	draw_sprite_ext(burgerSprite, 0, 50, 275, 4, 4, 0, c_white, 1)
} else {
	draw_rectangle_color(leftX, upY + spacing*0, rightX - 95, lowerY + spacing*0, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(raidPower){
	draw_rectangle_color(leftX, upY + spacing*1, rightX, lowerY + spacing*1, mainColor, edgeColor, mainColor, mainColor, false)
} else {
	draw_rectangle_color(leftX, upY + spacing*1, rightX - 95, lowerY + spacing*1, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(shavePower){
	draw_rectangle_color(leftX, upY + spacing*2, rightX, lowerY + spacing*2, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*2, rightX - 95, lowerY + spacing*2, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(phonePower){
	draw_rectangle_color(leftX, upY + spacing*3, rightX, lowerY + spacing*3, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*3, rightX - 95, lowerY + spacing*3, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(datingPower){
	draw_rectangle_color(leftX, upY + spacing*4, rightX, lowerY + spacing*4, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*4, rightX - 95, lowerY + spacing*4, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(wandLargePower){
	draw_rectangle_color(leftX, upY + spacing*5, rightX, lowerY + spacing*5, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*5, rightX - 95, lowerY + spacing*5, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(runnerPower){
	draw_rectangle_color(leftX, upY + spacing*6, rightX, lowerY + spacing*6, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*6, rightX - 95, lowerY + spacing*6, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(clashPower){
	draw_rectangle_color(leftX, upY + spacing*7, rightX, lowerY + spacing*7, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*7, rightX - 95, lowerY + spacing*7, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(skinPower){
	draw_rectangle_color(leftX, upY + spacing*8, rightX, lowerY + spacing*8, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*8, rightX - 95, lowerY + spacing*8, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(bugPower){
	draw_rectangle_color(leftX, upY + spacing*9, rightX, lowerY + spacing*9, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*9, rightX - 95, lowerY + spacing*9, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(crashPower){
	draw_rectangle_color(leftX, upY + spacing*10, rightX, lowerY + spacing*10, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*10, rightX - 95, lowerY + spacing*10, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}

if(BSODPower){
	draw_rectangle_color(leftX, upY + spacing*11, rightX, lowerY + spacing*11, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
} else {
	draw_rectangle_color(leftX, upY + spacing*11, rightX - 95, lowerY + spacing*11, mainColor, edgeColor, mainColor, mainColor, false) //what a tease, more powers await
}





